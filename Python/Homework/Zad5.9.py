tables = [["A","B","C"],["u","d","e"],["f","G","H"]]

length = len(tables[0])

for table in tables:
    for i in range(0,length):
        table[i] = table[i].lower()

print(tables)
































































